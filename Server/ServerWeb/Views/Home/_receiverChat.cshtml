@using ChatCore.Engine
@model ChatObject
<div class="d-flex justify-content-start mb-4">
    <div class="img_cont_msg">
        @Html.Identicon(Model.ReceiverName, 60)
    </div>
    <div class="msg_cotainer">
        @Model.Message
        <span class="msg_time">@Model.CreatedOn</span>
    </div>
</div>